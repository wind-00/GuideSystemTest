# 悬浮窗模块修改计划

## 1. 修改悬浮窗布局文件

修改 `orchestrator/src/main/res/layout/overlay_view.xml` 文件：

- 将按钮文字改为中文：
  - `Execute` → `执行`
  - `Stop` → `停止`

- 将页面ID显示改为中文：
  - `Current Page: Unknown` → `当前页面: 未知`

- 将输入框提示文字改为中文：
  - `Enter your command` → `输入您的指令`

- 将状态显示改为中文：
  - `Status: IDLE` → `状态: 空闲`

- 添加收起/展开按钮：
  - 在悬浮窗顶部添加一个收起/展开按钮

## 2. 修改 OverlayView.kt 文件

修改 `orchestrator/overlay/OverlayView.kt` 文件：

- 添加收起/展开功能的实现：
  - 添加一个变量来跟踪当前是否处于收起状态
  - 实现收起/展开按钮的点击事件处理
  - 当收起时，只显示最小化的悬浮窗图标
  - 当展开时，显示完整的悬浮窗内容

- 保持可移动功能不变：
  - 保留现有的触摸事件处理逻辑，确保悬浮窗可以拖动

## 3. 修改 OverlayService.kt 文件

修改 `orchestrator/overlay/OverlayService.kt` 文件：

- 确保服务正常运行：
  - 保持现有服务结构不变
  - 确保服务能够正确启动和停止

## 4. 测试修改后的悬浮窗功能

测试修改后的悬浮窗功能：

- 确保悬浮窗可以拖动
- 确保悬浮窗可以收起/展开
- 确保按钮文字和其他文本都是中文
- 确保悬浮窗的功能正常（执行指令、停止执行等）

## 注意事项

- 保持第一层级结构不变，不修改现有模块的整体架构
- 只修改悬浮窗相关的代码，不影响其他模块的功能
- 确保修改后的代码与现有代码风格一致
- 确保修改后的悬浮窗在不同设备上都能正常显示和使用